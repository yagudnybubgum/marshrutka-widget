# Route 605 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add route 605 (Павлово / ул. Быкова ↔ МЕГА Дыбенко) as a tab with nearest + full schedule, mirroring 664.

**Architecture:** Daily two-column XLSX → `convert-schedules` JSON; register in `ROUTES` with `gray`; exclude from «С Ладожской»; direction formatter for Павлово.

**Tech Stack:** Vite/React, existing schedule pipeline (`xlsx` → JSON), Vitest.

## Global Constraints

- Destination: `ул. Быкова (Павлово)`
- Directions: `Из Павлово, ул. Быкова` | `От "МЕГА Дыбенко"`
- Color: `gray` (reuse, no new token)
- No `routes-geo/605.json`
- Exclude from `ROUTES_FROM_LADOZHSKAYA` like `664`
- XLSX column order: Павлово first, МЕГА second

## File map

| File | Role |
| --- | --- |
| `public/schedule-605.xlsx` | Source schedule |
| `src/config/routes.js` | Route registry + Ladozhskaya filter |
| `src/config/copy.js` | `fromPavlovo` string |
| `src/utils/schedule/formatDirectionName.js` | Павлово/Быкова → copy |
| `src/utils/schedule/schedule.test.js` | Formatter test |
| `src/App.jsx` | Legacy `/full605` redirect |
| `README.md` | Route list |

---

### Task 1: Direction copy + formatter (TDD)

**Files:**
- Modify: `src/config/copy.js`
- Modify: `src/utils/schedule/formatDirectionName.js`
- Modify: `src/utils/schedule/schedule.test.js`

**Interfaces:**
- Produces: `copy.direction.fromPavlovo === 'Из Павлово, ул. Быкова'`
- Produces: `formatDirectionName('Павлово')` / `'ул. Быкова'` → that string
- Produces: existing Mega mapping unchanged

- [x] **Step 1: Failing test**
- [x] **Step 2: Run test — expect FAIL**
- [x] **Step 3: Implement**
- [x] **Step 4: Run tests — expect PASS**

---

### Task 2: Register route + schedule file

**Files:**
- Create: `public/schedule-605.xlsx`
- Modify: `src/config/routes.js`
- Modify: `src/App.jsx`
- Modify: `README.md`

**Interfaces:**
- Consumes: schedule times from spec
- Produces: `isValidRouteId('605') === true`
- Produces: `ROUTES_FROM_LADOZHSKAYA` does not include 605
- Produces: `public/schedules/605.json` via convert

- [x] **Step 1: Create XLSX**
- [x] **Step 2: Register config**
- [x] **Step 3: Convert + smoke**
- [x] **Step 4: Manual check** — http://localhost:5173/?tab=605 and /full/605

---

## Spec coverage

- Schedule times ✓ Task 2  
- ROUTES + gray + destination ✓ Task 2  
- Exclude Ladozhskaya ✓ Task 2  
- LEGACY_FULL ✓ Task 2  
- Direction copy + formatter ✓ Task 1  
- No geo ✓ (omitted)  
- README ✓ Task 2  
